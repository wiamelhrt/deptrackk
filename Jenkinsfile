pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                sh 'git clone https://github.com/wiamelhrt/deptrackk.git'
            }
        }
    }
}

